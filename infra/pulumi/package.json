{
  "name": "gatrr-infra",
  "version": "0.1.0",
  "description": "Pulumi infrastructure for portal deployment",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src",
    "clean": "rm -rf dist",
    "config:load": "npx ts-node scripts/load-config.ts",
    "new": "npx ts-node scripts/new-stack.ts",
    "new:pulumiCloud": "npx ts-node scripts/new-pulumi-cloud.ts",
    "generate:types": "npx ts-node scripts/generate-types.ts"
  },
  "dependencies": {
    "@pulumi/docker": "^4.5.0",
    "@pulumi/pulumi": "^3.100.0",
    "@pulumi/random": "^4.18.5",
    "ajv": "^8.17.1",
    "js-yaml": "^4.1.1",
    "yaml": "^2.3.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.10.0",
    "eslint": "^9.17.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0",
    "typescript-eslint": "^8.18.0",
    "vitest": "^1.0.0"
  }
}
